export interface PaymentMethod {
    _id: string
    name: string // Tên phương thức thanh toán (Ví dụ: Credit Card, PayPal, ...)
    description: string // Mô tả
    createdAt: string
    updatedAt: string
}
